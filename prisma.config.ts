/**
 * prisma.config.ts
 *
 * This file is intentionally minimal. Prisma 5.x does not require a
 * prisma.config.ts — all configuration is handled in prisma/schema.prisma
 * via the generator and datasource blocks.
 *
 * If upgrading to Prisma 6+, refer to the major version upgrade guide:
 * https://pris.ly/d/major-version-upgrade
 */

export {};
